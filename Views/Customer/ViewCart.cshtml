@model FridgeManagementSystem.Models.Cart
@{
    Layout = "_Layout";
    ViewData["Title"] = "Your Cart";
}

<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

<h2 class="mb-4">Your Shopping Cart</h2>

@if (Model.CartItems == null || !Model.CartItems.Any())
{
    <div class="alert alert-info">Your cart is empty.</div>
}
else
{
    <div class="table-responsive shadow-sm rounded">
        <table class="table table-hover align-middle mb-4">
            <thead class="table-light">
                <tr>
                    <th>Fridge</th>
                    <th>Price</th>
                    <th>Quantity</th>
                    <th>Subtotal</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.CartItems)
                {
                    <tr>
                        <td>
                            <div class="d-flex align-items-center">
                                <img src="/images/fridges/@item.Fridge.ImageUrl" style="width:60px; height:60px; object-fit:cover; margin-right:10px;" />
                                <span>@item.Fridge.Brand</span>
                            </div>
                        </td>
                        <td>@item.Fridge.Price.ToString("C")</td>
                        <td>@item.Quantity</td>
                       <td>@((item.Fridge.Price * item.Quantity).ToString("C"))</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>

    <div class="d-flex justify-content-between align-items-center mb-4">
        <h4>Total: @Model.CartItems.Sum(i => i.Fridge.Price * i.Quantity).ToString("C")</h4>
        <a asp-action="Checkout" class="btn btn-primary btn-lg">Proceed to Checkout</a>
    </div>
}

