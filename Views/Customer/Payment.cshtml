@model FridgeManagementSystem.ViewModels.PaymentViewModel
@{
    Layout = "_Layout";
    ViewData["Title"] = "Payment";
}

<div class="container mt-5">
    <h2 class="fw-bold mb-4 text-center">Payment Details</h2>

    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow-sm border-0 p-4">
                <form asp-action="AddCard" method="post" enctype="multipart/form-data">
                    @Html.AntiForgeryToken()

                    <input type="hidden" asp-for="OrderId" />
                    <input type="hidden" asp-for="Amount" />
                    <input type="hidden" asp-for="Method" value="Card" />

                    <div class="mb-3">
                        <label class="form-label fw-bold">Cardholder Name</label>
                        <input asp-for="CardholderName" class="form-control" placeholder="Full name on card" required />
                    </div>

                    <div class="mb-3">
                        <label class="form-label fw-bold">Card Number</label>
                        <input asp-for="CardNumber" class="form-control" placeholder="1234 5678 9012 3456" maxlength="19" required />
                    </div>

                    <div class="row mb-3">
                        <div class="col-md-6">
                            <label class="form-label fw-bold">Expiry Date</label>
                            <input asp-for="ExpiryDate" class="form-control" placeholder="MM/YY" required />
                        </div>
                        <div class="col-md-6">
                            <label class="form-label fw-bold">CVV</label>
                            <input asp-for="CVV" class="form-control" type="password" maxlength="3" placeholder="123" required />
                        </div>
                    </div>

                    <div class="d-grid">
                        <button type="submit" class="btn btn-success btn-lg">Pay @Model.Amount.ToString("C")</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        // format card number input with spaces
        document.querySelector('input[name="CardNumber"]').addEventListener('input', function (e) {
            e.target.value = e.target.value.replace(/\D/g, '').replace(/(.{4})/g, '$1 ').trim();
        });
    </script>
}



@* @model FridgeManagementSystem.ViewModels.PaymentViewModel
@{
    Layout = "_Layout";
    ViewData["Title"] = "Payment";
}

<h2 class="mb-4 text-center">Payment</h2>

<div class="row justify-content-center">
    <div class="col-md-6">
        <div class="card shadow-sm p-4">
            <h4 class="mb-3">Order #@Model.OrderId - Amount: @Model.Amount.ToString("C")</h4>

            <form asp-action="ProcessPayment" method="post">
                <input type="hidden" asp-for="OrderId" />
                <input type="hidden" asp-for="Amount" />

                <div class="mb-3">
                    <label asp-for="Method" class="form-label">Payment Method</label>
                    <select asp-for="Method" class="form-select">
                        <option value="">Select Method</option>
                        <option value="Card">Card</option>
                        <option value="EFT">EFT</option>
                        <option value="CashOnDelivery">Cash on Delivery</option>
                        <option value="Wallet">Wallet / Store Credit</option>
                    </select>
                    <span asp-validation-for="Method" class="text-danger"></span>
                </div>

                <div class="mb-3" id="card-details" style="display:none;">
                    <label class="form-label">Card Number</label>
                    <input type="text" class="form-control" placeholder="xxxx-xxxx-xxxx-xxxx" />
                    <label class="form-label mt-2">Expiry</label>
                    <input type="month" class="form-control" />
                    <label class="form-label mt-2">CVV</label>
                    <input type="text" class="form-control" placeholder="xxx" />
                </div>

                <button type="submit" class="btn btn-success w-100 mt-3">Pay Now</button>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        const methodSelect = document.querySelector('select[name="Method"]');
        const cardDetails = document.getElementById('card-details');

        methodSelect.addEventListener('change', () => {
            if (methodSelect.value === 'Card') {
                cardDetails.style.display = 'block';
            } else {
                cardDetails.style.display = 'none';
            }
        });
    </script>
}
 *@